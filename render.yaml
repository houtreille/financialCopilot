services:
  - type: web
    name: financial-copilot-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      # Neon Postgres. Set these in the Render dashboard, not here.
      # SPRING_DATASOURCE_URL must use the "-pooler" endpoint host and
      # include "?sslmode=require" - see application-prod.yml for details.
      - key: SPRING_DATASOURCE_URL
        sync: false
      - key: SPRING_DATASOURCE_USERNAME
        sync: false
      - key: SPRING_DATASOURCE_PASSWORD
        sync: false

  - type: web
    name: financial-copilot-frontend
    runtime: static
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: ./frontend/dist/frontend/browser
    routes:
      - type: rewrite
        source: /api/*
        destination: https://financial-copilot-backend.onrender.com/api/*
      - type: rewrite
        source: /*
        destination: /index.html
